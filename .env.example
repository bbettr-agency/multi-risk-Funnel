# GoHighLevel inbound webhook that receives quote leads.
# In GHL: create a Workflow → trigger "Inbound Webhook" → copy the URL here.
# The workflow should (a) email the lead to the client and (b) create/update the contact.
GHL_WEBHOOK_URL=
